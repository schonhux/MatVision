# MatVision worker — installs on top of api/app's own deps (copied into this image
# at build time, see worker/Dockerfile). ffmpeg itself is a system package, installed
# via apt in the Dockerfile, not pip.
dramatiq[redis]>=1.17
redis>=5.0
sqlalchemy>=2.0
psycopg2-binary>=2.9
pydantic>=2.7
pydantic-settings>=2.3
boto3>=1.34
ffmpeg-python>=0.2.0

# --- Layer 3: computer vision -------------------------------------------------
# torch installs with MPS support on Apple Silicon via the default index. For a
# CPU-only build (CI), use: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
torchvision>=0.17
ultralytics>=8.2          # YOLO detection + YOLOv8-pose (ADR-006)
supervision>=0.29         # ByteTrack (no torch dependency of its own)
opencv-python-headless>=4.9
numpy>=1.26
pandas>=2.2
pyarrow>=15.0             # parquet artifacts
lightgbm>=4.5
scikit-learn>=1.5
joblib>=1.4
