# MatVision — ML environment (Layer 0+)
# Install with:  pip install -r ml/requirements.txt
#
# Runs on Apple Silicon (torch picks up MPS automatically via the default PyPI index).
# For CPU-only CI/sandboxes, install torch from the CPU index instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

torch>=2.2
torchvision>=0.17
ultralytics>=8.2         # YOLO detection + YOLOv8-pose (see dev/DECISIONS.md ADR-006)
supervision>=0.29        # ByteTrack + detection/track utilities (no torch dependency itself)
opencv-python-headless>=4.9
pandas>=2.2
pyarrow>=15.0             # parquet artifact storage
numpy>=1.26
matplotlib>=3.8           # diagnostic plots (ID-hold over time, etc.)
tqdm>=4.66
pytest>=8.0               # test suite (tracking metrics, feature math)
lightgbm>=4.5
scikit-learn>=1.5
joblib>=1.4
mlflow>=2.16
anthropic>=0.34         # Layer 6: report LLM (see dev/DECISIONS.md — Claude API)
